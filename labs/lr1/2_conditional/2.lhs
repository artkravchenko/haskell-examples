  Задание 2.2
  Напишите функцию, возвращающую модуль целого числа.

> abs' :: Integral a => a -> a
> abs' x | x >= 0 = x
>        | True   = -x

> test =    all (\x -> abs x == x)  [0, 1, 1e9, 1e12]
>        && all (\x -> abs x == -x) [-1, -1e9, -1e12]
